name: Validate python-3-12-red-quartz Dockerfile

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - python/3.12/red-quartz/Dockerfile
      - .github/workflows/python-3-12-red-quartz-pr-build.yml

jobs:
  build-only:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: python/3.12/red-quartz/Dockerfile
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: python:3.12-redquartz-pr-${{ github.sha }}
