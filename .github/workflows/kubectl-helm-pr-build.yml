name: Validate kubectl-helm Dockerfile

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    paths:
      - kubectl-helm/Dockerfile
      - .github/workflows/kubectl-helm-pr-build.yml

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: kubectl-helm/Dockerfile
          push: false
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: kubectl-helm:pr-${{ github.sha }}

      - name: Verify kubectl and helm in final image
        run: docker run --rm kubectl-helm:pr-${{ github.sha }} /bin/sh -lc "kubectl version --client && helm version"
